---
format_version: 1
repo: app-poc-2
---

## Repository identity

app-poc-2 is a Kotlin / Ktor backend API running on the JVM. Its routes
(`GET /` and `GET /health`) are consumed by app-poc-1. It is a product repo
within the agentic pipeline — all workflows are inherited from
agentic-pipeline.

## Repo-specific behavioral notes

(Populated by agent executions via the memory-write skill.)
